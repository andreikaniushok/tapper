import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  timer: {
    fontSize: 24,
    fontWeight: "bold",
    color: "black",
    textAlign: "center",
    marginBottom: 20,
  },
});

export default styles;
